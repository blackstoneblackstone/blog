<% if( post.tags && post.tags.length ) { %>
<div class="article-tag-list">
    <i class="icon-tag"></i>
    <%- list_tags(post.tags, {
        style: true,
        class: 'article-tag',
        show_count: false
    }) %>
</div>
<% } %>
